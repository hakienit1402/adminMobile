import React from 'react'

const historyReducer = (state={},action) => {
    switch (action.type) {
        case 'ADD_HISTORY':
            
            break;
    
        default:
            return state
    }
}

export default historyReducer
