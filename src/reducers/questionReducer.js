import React from 'react'

const questionReducer = (state={},action) => {
  switch (action.type) {
      case 'ADD_QUESTION':
          
          break;
  
      default:
          return state
  }
}

export default questionReducer
